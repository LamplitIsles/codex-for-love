export type DockEdge = 'left' | 'right' | 'top' | 'bottom'; export type DockPosition = { edge: DockEdge; offset: number }; export type Viewport = { width: number; height: number; size: number }; const margin = 12;
export function defaultDock(view: Viewport): DockPosition { return { edge: 'right', offset: Math.max(margin, view.height - view.size - 88) }; }
export function dockPoint(position: DockPosition, view: Viewport): { x: number; y: number } { const offset = Math.max(margin, Math.min(position.offset, (position.edge === 'left' || position.edge === 'right' ? view.height : view.width) - view.size - margin)); return position.edge === 'left' ? { x: margin, y: offset } : position.edge === 'right' ? { x: view.width - view.size - margin, y: offset } : position.edge === 'top' ? { x: offset, y: margin } : { x: offset, y: view.height - view.size - margin }; }
export function boundedPoint(point: { x: number; y: number }, view: Viewport): { x: number; y: number } { return { x: Math.max(margin, Math.min(point.x, view.width - view.size - margin)), y: Math.max(margin, Math.min(point.y, view.height - view.size - margin)) }; }
export function snapDock(point: { x: number; y: number }, view: Viewport): DockPosition { const p = boundedPoint(point, view); const distances: Array<[number, DockEdge]> = [[p.x, 'left'], [view.width - view.size - p.x, 'right'], [p.y, 'top'], [view.height - view.size - p.y, 'bottom']]; const edge = distances.reduce((a, b) => a[0] <= b[0] ? a : b)[1]; return { edge, offset: edge === 'left' || edge === 'right' ? p.y : p.x }; }
export function keyboardDock(position: DockPosition, key: string, view: Viewport): DockPosition | undefined { if (key === 'Home') return { edge: 'left', offset: margin }; if (key === 'End') return defaultDock(view); const point = dockPoint(position, view); const delta = 20; if (key === 'ArrowLeft') point.x -= delta; else if (key === 'ArrowRight') point.x += delta; else if (key === 'ArrowUp') point.y -= delta; else if (key === 'ArrowDown') point.y += delta; else return undefined; return snapDock(point, view); }
